import ReactDOM from "react-dom/client";
import "./index.module.scss";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);

root.render(<h1 className="hello">Hello</h1>);

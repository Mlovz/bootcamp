import React from "react";
import cls from "./header.module.scss";

const Header = () => {
  return (
    <header className={cls.header}>
      {/* <Button variant="solid">Asadasd</Button> */}
    </header>
  );
};

export default Header;
